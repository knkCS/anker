import { Canvas, Meta, ArgTypes } from "@storybook/blocks";
import * as Stories from "./slug-cell.stories";

<Meta of={Stories} />

# SlugCell

Displays a URL slug value rendered as inline `<code>` text for easy visual distinction from plain text columns.

## Usage

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />
