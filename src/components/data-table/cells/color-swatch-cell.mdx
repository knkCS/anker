import { Canvas, Meta, ArgTypes } from "@storybook/blocks";
import * as Stories from "./color-swatch-cell.stories";

<Meta of={Stories} />

# ColorSwatchCell

Displays a color value as a small circular swatch alongside the hex (or CSS) color string. Accepts any valid CSS color value.

## Usage

<Canvas of={Stories.Default} />

## Props

<ArgTypes of={Stories} />
